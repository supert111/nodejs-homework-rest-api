const contactsSchema = require('./contactcSchema');
const contactPatchSchema = require('./contactPatchSchema');
const signupSchema = require('./signupSchema');

module.exports = {
  contactsSchema,
  contactPatchSchema,
  signupSchema,
}
